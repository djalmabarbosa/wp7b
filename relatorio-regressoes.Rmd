---
title: " "
---

<br/><br/>

```{r teste de sobredispersão, eval=F, echo=FALSE, warning=F, message=F,results='asis', fig.align='center'}

dadoswp7a <- readRDS("dadoswp7a.RDS")

library(AER)

dados <- as.data.frame(dadoswp7a$variables)

rd <- glm(as.numeric(FezTaix12m) ~ ., data = dados, family = poisson)
dispersiontest(rd,trafo=1)

# Não rejeitou a hipótese nula de equidispersão.

```

### Preditores da Prática de Meditação 
<br/>
<div align="center">
```{r eval=T, echo=FALSE, warning=F, message=F,results='asis', fig.align='center'}

dadoswp7a <- readRDS("dadoswp7a.RDS")

library(survey)

logit1 <- (svyglm(FezMeditacao12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc,
                  family=quasibinomial, design=dadoswp7a))

logit2 <- (svyglm(FezMeditacao12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude,
                 family=quasibinomial, design=dadoswp7a))

logit3 <- (svyglm(FezMeditacao12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas,
                 family=quasibinomial, design=dadoswp7a))

logit4 <- (svyglm(FezMeditacao12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas+
                 Tabagismo+FreqAlcool1m+Exercicio1s,
                 family=quasibinomial, design=dadoswp7a))

se.coef1 <- function(logit1){sqrt(diag(vcov(logit1)))}
OR1 <- exp(coef(logit1))
Std.Error.OR1 <-  OR1*se.coef1(logit1)

se.coef2 <- function(logit2){sqrt(diag(vcov(logit2)))}
OR2 <- exp(coef(logit2))
Std.Error.OR2 <-  OR2*se.coef2(logit2)

se.coef3 <- function(logit3){sqrt(diag(vcov(logit3)))}
OR3 <- exp(coef(logit3))
Std.Error.OR3 <-  OR3*se.coef3(logit3)

se.coef4 <- function(logit4){sqrt(diag(vcov(logit4)))}
OR4 <- exp(coef(logit4))
Std.Error.OR4 <-  OR4*se.coef4(logit4)

Notes <- "<span>&#9768;&#9768;&#9768;</span>: p<0.01; <span>&#9768;&#9768;</span>: p<0.05; <span>&#9768;</span>: p<0.1"

library(stargazer)
stargazer(logit1,logit2,logit3,logit4,
          coef=list(OR1,OR2,OR3,OR4), se = list(Std.Error.OR1,Std.Error.OR2,Std.Error.OR3,Std.Error.OR4), 
          type='html', header = F,ci=T,intercept.bottom=F,intercept.top = F,
          notes=Notes,notes.append=F,
          notes.label = "Coeficientes expressam Odds Ratio",
          column.labels = c("Demográficas","+Serv. de Saúde","+Est. de Saúde","+Est. de Vida"),
          column.sep.width = "7pt",
          single.row = F,dep.var.caption="Variável Dependente", star.char = "&#9768;"
          )

```
</div>
<br/><br/>

### Preditores da Prática de Yoga
<br/>
<div align="center">
```{r eval=T, echo=FALSE, warning=F, message=F,results='asis', fig.align='center'}

dadoswp7a <- readRDS("dadoswp7a.RDS")

library(survey)

logit1 <- (svyglm(FezYoga12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc,
                  family=quasibinomial, design=dadoswp7a))

logit2 <- (svyglm(FezYoga12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude,
                 family=quasibinomial, design=dadoswp7a))

logit3 <- (svyglm(FezYoga12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas,
                 family=quasibinomial, design=dadoswp7a))

logit4 <- (svyglm(FezYoga12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas+
                 Tabagismo+FreqAlcool1m+Exercicio1s,
                 family=quasibinomial, design=dadoswp7a))


se.coef1 <- function(logit1){sqrt(diag(vcov(logit1)))}
OR1 <- exp(coef(logit1))
Std.Error.OR1 <-  OR1*se.coef1(logit1)

se.coef2 <- function(logit2){sqrt(diag(vcov(logit2)))}
OR2 <- exp(coef(logit2))
Std.Error.OR2 <-  OR2*se.coef2(logit2)

se.coef3 <- function(logit3){sqrt(diag(vcov(logit3)))}
OR3 <- exp(coef(logit3))
Std.Error.OR3 <-  OR3*se.coef3(logit3)

se.coef4 <- function(logit4){sqrt(diag(vcov(logit4)))}
OR4 <- exp(coef(logit4))
Std.Error.OR4 <-  OR4*se.coef4(logit4)

Notes <- "<span>&#9768;&#9768;&#9768;</span>: p<0.01; <span>&#9768;&#9768;</span>: p<0.05; <span>&#9768;</span>: p<0.1"

library(stargazer)
stargazer(logit1,logit2,logit3,logit4,
          coef=list(OR1,OR2,OR3,OR4), se = list(Std.Error.OR1,Std.Error.OR2,Std.Error.OR3,Std.Error.OR4), 
          type='html', header = F,ci=T,intercept.bottom=F,intercept.top = F,
          notes=Notes,notes.append=F,
          notes.label = "Coeficientes expressam Odds Ratio",
          column.labels = c("Demográficas","+Serv. de Saúde","+Est. de Saúde","+Est. de Vida"),
          column.sep.width = "7pt",
          single.row = F,dep.var.caption="Variável Dependente", star.char = "&#9768;"
          )

```
</div>
<br/><br/>

### Preditores da Prática de Tai chi chuan ou Lian gong ou Qi gong
<br/>
<div align="center">
```{r eval=T, echo=FALSE, warning=F, message=F,results='asis', fig.align='center'}

dadoswp7a <- readRDS("dadoswp7a.RDS")

library(survey)

logit1 <- (svyglm(FezTaix12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc,
                  family=quasibinomial, design=dadoswp7a))

logit2 <- (svyglm(FezTaix12m~Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude,
                 family=quasibinomial, design=dadoswp7a))

logit3 <- (svyglm(FezTaix12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas,
                 family=quasibinomial, design=dadoswp7a))

logit4 <- (svyglm(FezTaix12m~Regiao+Sexo+Idade+CorRaca3+EmRelacionamento+Regiao+NivelEducacional+RendaFamiliarPc+
                 VisitasDoAC12m+TemPlanoSaude+
                 DoencasCronicas+
                 Tabagismo+FreqAlcool1m+Exercicio1s,
                 family=quasibinomial, design=dadoswp7a))

se.coef1 <- function(logit1){sqrt(diag(vcov(logit1)))}
OR1 <- exp(coef(logit1))
Std.Error.OR1 <-  OR1*se.coef1(logit1)

se.coef2 <- function(logit2){sqrt(diag(vcov(logit2)))}
OR2 <- exp(coef(logit2))
Std.Error.OR2 <-  OR2*se.coef2(logit2)

se.coef3 <- function(logit3){sqrt(diag(vcov(logit3)))}
OR3 <- exp(coef(logit3))
Std.Error.OR3 <-  OR3*se.coef3(logit3)

se.coef4 <- function(logit4){sqrt(diag(vcov(logit4)))}
OR4 <- exp(coef(logit4))
Std.Error.OR4 <-  OR4*se.coef4(logit4)

Notes <- "<span>&#9768;&#9768;&#9768;</span>: p<0.01; <span>&#9768;&#9768;</span>: p<0.05; <span>&#9768;</span>: p<0.1"

library(stargazer)
stargazer(logit1,logit2,logit3,logit4,
          coef=list(OR1,OR2,OR3,OR4), se = list(Std.Error.OR1,Std.Error.OR2,Std.Error.OR3,Std.Error.OR4), 
          type='html', header = F,ci=T,intercept.bottom=F,intercept.top = F,
          notes=Notes,notes.append=F,
          notes.label = "Coeficientes expressam Odds Ratio",
          column.labels = c("Demográficas","+Serv. de Saúde","+Est. de Saúde","+Est. de Vida"),
          column.sep.width = "7pt",
          single.row = F,dep.var.caption="Variável Dependente", star.char = "&#9768;"
          )

```
</div>
